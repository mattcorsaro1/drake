package drake;

// Status containing the floor's joint position and velocity.
struct lcmt_slider_status {
  // The timestamp in microseconds.
  int64_t utime;

  // Full state contains position and velocity (2 values)
  int32_t num_state_vals;
  double slider_state[num_state_vals];
}
